# SSH Project

This directory contains a Bash script for connecting to a remote server via SSH using a private key.

## 0-use_a_private_key
This script connects to a server using:
- The private key located at `~/.ssh/school`
- The `ubuntu` user

Usage:

